meta {
  name: Visitor cant attmpt quiz
  type: http
  seq: 26
}

post {
  url: http://{{ilim-url}}/student/attempt-quiz
  body: json
  auth: bearer
}

auth:bearer {
  token: 
}

body:json {
  {
    "quizId": "{{quizId}}",
    "answers": [
      {
        "questionId": "{{questions1Id}}",
        "selectedOptionIds": [
          "{{questions1Option2}}"
        ]
      },
      {
        "questionId": "{{questions2Id}}",
        "selectedOptionIds": [
          "{{questions2Option1}}",
          "{{questions2Option3}}"
        ]
      }
    ]
  }
}

assert {
  res.status: eq 401
}
