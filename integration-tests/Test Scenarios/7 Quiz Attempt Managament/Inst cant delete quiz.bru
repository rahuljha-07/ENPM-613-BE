meta {
  name: Inst cant delete quiz
  type: http
  seq: 27
}

delete {
  url: http://{{ilim-url}}/instructor/delete-quiz/{{quizId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{instructorToken}}
}

body:json {
  {
    "title": "Intro to Module (test run)",
    "description": "edited={{moduleId}}"
  }
}

assert {
  res.status: eq 403
}
